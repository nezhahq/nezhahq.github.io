import{_ as o,c as e,o as t,a4 as r}from"./chunks/framework.BmdFiWrL.js";const P=JSON.parse('{"title":"服务监控","description":"","frontmatter":{"outline":"deep"},"headers":[],"relativePath":"guide/services.md","filePath":"guide/services.md","lastUpdated":1734688054000}'),i={name:"guide/services.md"},l=r('<h1 id="服务监控" tabindex="-1">服务监控 <a class="header-anchor" href="#服务监控" aria-label="Permalink to &quot;服务监控&quot;">​</a></h1><p><strong>服务区域是设置 Agent 监控外部网站或服务器功能的设置区</strong>。<br> 通过已设置的服务监控，你可以在主页中点击 <strong><code>服务</code></strong> 图标，查看过去 30 天的可用性监控结果。</p><hr><h2 id="使用方法" tabindex="-1">使用方法 <a class="header-anchor" href="#使用方法" aria-label="Permalink to &quot;使用方法&quot;">​</a></h2><h3 id="新增服务监控" tabindex="-1">新增服务监控 <a class="header-anchor" href="#新增服务监控" aria-label="Permalink to &quot;新增服务监控&quot;">​</a></h3><p>要新增一个服务监控，请按照以下步骤操作：</p><ol><li><p><strong>进入管理面板</strong><br> 转到 <strong><code>服务</code></strong> 页，点击 <strong><code>+</code></strong> 按钮添加新的服务监控。</p></li><li><p><strong>配置参数</strong><br> 在新增监控时，设置以下参数：</p><ul><li><strong>名称</strong>：为此服务自定义一个名称。</li><li><strong>目标</strong>：根据监控类型设置目标： <ul><li><code>HTTP GET</code>：输入完整 URL（需包含 <code>http://</code> 或 <code>https://</code>），例如：<code>https://example.com</code>。<br><strong>注意</strong>：如果目标 URL 使用 <code>https://</code>，哪吒监控将同时监控该 URL 的 SSL 证书（如证书到期或变更将触发通知）。</li><li><code>ICMP Ping</code>：输入域名或 IP 地址（不含端口号），例如：<code>1.1.1.1</code> 或 <code>example.com</code>。</li><li><code>TCP Ping</code>：输入域名或 IP 地址并添加端口号，例如：<code>1.1.1.1:80</code> 或 <code>example.com:22</code>。</li></ul></li><li><strong>类型</strong>：选择监控类型（<code>HTTP GET</code>、<code>ICMP Ping</code> 或 <code>TCP Ping</code>）。</li><li><strong>服务中显示</strong>：选择是否向游客显示此监控（隐私性选项）。</li><li><strong>间隔</strong>：设置监控间隔时间（单位：秒）。</li><li><strong>覆盖范围</strong>：选择一条规则来确定哪些 Agent 请求目标。</li><li><strong>特定服务器</strong>：指定覆盖范围内需要使用的 Agent。</li><li><strong>通知组 ID</strong>：选择已在 <strong><code>通知</code></strong> 页中配置的通知方式，详见 <a href="/guide/notifications.html#灵活的通知方式">通知方式配置</a>。</li><li><strong>启用失败通知</strong>：可选择在目标故障时接收通知（默认为不启用）。</li></ul></li><li><p><strong>提交监控</strong><br> 点击 <strong><code>提交</code></strong> 按钮保存配置，稍等片刻后返回主页查看监控结果。</p></li></ol><hr><h2 id="延迟变化通知" tabindex="-1">延迟变化通知 <a class="header-anchor" href="#延迟变化通知" aria-label="Permalink to &quot;延迟变化通知&quot;">​</a></h2><p>哪吒监控会统计并监测 Agent 到目标服务器之间的延迟。当延迟发生显著变化时，可以发送通知帮助你监控线路状态。</p><ul><li><strong>启用延迟通知</strong>：选择启用后，当延迟超出设置的范围（大于 <code>最高延迟</code> 或小于 <code>最低延迟</code>）时将发送通知。</li></ul><hr><h2 id="通知时触发任务" tabindex="-1">通知时触发任务 <a class="header-anchor" href="#通知时触发任务" aria-label="Permalink to &quot;通知时触发任务&quot;">​</a></h2><p>如果需要在服务监控触发通知时执行特定任务，可以：</p><ol><li>勾选 <strong><code>启用触发任务</code></strong>。</li><li>配置： <ul><li><strong>触发警报的任务</strong>：选择警报触发时执行的任务。</li><li><strong>恢复后要触发的任务</strong>：选择服务恢复正常后执行的任务。</li></ul></li></ol><p>提前在任务页中设置好任务配置后即可使用此功能。</p><hr><h2 id="网络延迟图表" tabindex="-1">网络延迟图表 <a class="header-anchor" href="#网络延迟图表" aria-label="Permalink to &quot;网络延迟图表&quot;">​</a></h2><p>对于 <code>TCP-Ping</code> 和 <code>ICMP-Ping</code> 类型的监控，Dashboard 会自动生成网络延迟统计图表：</p><ul><li>进入对应服务器的详情页，切换到 <strong><code>网络</code></strong> 标签。</li><li>查看历史网络延迟变化趋势，数据基于 Agent 到目标服务器的实时延迟统计生成。</li></ul><hr><h2 id="管理监控" tabindex="-1">管理监控 <a class="header-anchor" href="#管理监控" aria-label="Permalink to &quot;管理监控&quot;">​</a></h2><p>如需编辑或删除已有服务监控：</p><ol><li>进入管理面板中的 <strong><code>服务</code></strong> 页。</li><li>找到目标监控配置。</li><li>点击右侧的编辑或删除图标，进行修改或移除。</li></ol>',24),a=[l];function n(s,d,c,g,h,p){return t(),e("div",null,a)}const _=o(i,[["render",n]]);export{P as __pageData,_ as default};
