---
description: 哪吒监控 V2 Dashboard 数据迁移、备份与恢复流程，说明停机、/opt/nezha 数据目录、权限、版本兼容和恢复验证注意事项。
outline: deep
---

# 如何进行数据迁移、备份恢复？

按照以下步骤完成哪吒监控的数据迁移或备份恢复：

:::danger

本页只适用于源端和目标端均为兼容 V2 版本的迁移或备份恢复。若数据来自 V0 或 V1，请先查看对应 Release 和迁移说明；不要假定跨产品代际可以通过直接覆盖文件完成升级。

:::

1. **停止面板服务**  
   在旧服务器中运行一键脚本，选择 `停止面板`。

2. **备份数据**  
   打包旧服务器中的 `/opt/nezha` 文件夹，并将其复制到新服务器相同位置。

3. **恢复数据**  
   在新服务器中解压 `/opt/nezha` 文件夹至相同路径。

4. **启动面板服务**  
   在新服务器中运行一键脚本，选择 `启动面板`。

---

### 注意事项
- 确保 `/opt/nezha` 文件夹及其内容的权限和所有权在新服务器上正确设置。
- 数据迁移完成后，可以通过访问 Dashboard 界面验证迁移是否成功。
